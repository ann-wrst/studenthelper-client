import {API_BASE_URL} from "../constants/api"
import {Component} from "react";
class Deadlines extends Component {
    constructor(props) {
        super(props);
    }
    render() {
        return (<div>Deadlines here</div>);
    }
}
export default Deadlines;
