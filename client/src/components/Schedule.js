import {API_BASE_URL} from "../constants/api"
import {Component} from "react";
class Schedule extends Component {
    constructor(props) {
        super(props);
    }
    render() {
        return (<div>Schedule here</div>);
    }
}
export default Schedule;
